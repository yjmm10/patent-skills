# Paper Bridge

**Read** `paper-bridge.md`；润色阶段 **`Read`** `../paper-polish-skill/references/experiment-section-structure.md`（Section V 结构）。

## 步骤

1. 锁定批次 + 各 arm LATEST run
2. 从 metrics/config 提取 Table I 与主表数字（标注 ep）
3. 列出/运行 `scripts/fig_*.py` 输入输出
4. 起草或更新 `paper/section5_实验_v*.md`（4 节结构）
5. 生成交接包 → 建议用户 invoke **`paper-polish-skill`** experiment + code_abstraction 模式

## 交付

- section5 草稿或 diff
- 交接包（见 paper-bridge.md）
- 数字溯源表：| 指标 | 值 | arm | ep | metrics 行 |
